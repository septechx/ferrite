autoupdate: bool,
key_store: { DotEnv, Pass }
server:
  wrapper: String
  executable: String
ferium:
  game_versions:
  - String
  mod_loaders:
  - String
  overrides:
    # Modrinth / Curseforge mod
    String: String
    # Github releases mod
    String:
      - String
      - String
  mods:
    # Modrinth / Curseforge mod
    - name: String
      identifier: String
      slug: String
    # Github releases mod
    - name: String
      identifier:
        - String
        - String
      slug: String
  disabled:
    # Modrinth / Curseforge mod
    - name: String
      identifier: String
      slug: String
    # Github releases mod
    - name: String
      identifier:
        - String
        - String
      slug: String
